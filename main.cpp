#pragma once
#include <generic.hpp>
#include <shapes.hpp>

int main(){
  auto s1 = sim::Shape({1, 0});
  auto s2 = sim::Shape({0, 1});
  auto a  = sim::agenda();

  sim::____();
  sim::display(1, "test", 3);
  sim::____();
  return 0
}
